/** Build-time constants injected by `vite.config.ts` (`define`). Undefined in
 * vitest and in any bundle that did not define them — always read through
 * `typeof … === "undefined"` guards. */
declare const __APOLLO_RUNTIME_PROXY__: string | undefined;
